create or replace TABLE RETAIL_PROJECT.RAW_STAGE.CUSTOMERS (
	CUSTOMER_ID NUMBER(38,0),
	NAME VARCHAR(16777216),
	EMAIL VARCHAR(16777216),
	CITY VARCHAR(16777216),
	SIGNUP_DATE DATE,
	CUSTOMER_SEGMENT VARCHAR(16777216)
);

create or replace TABLE RETAIL_PROJECT.RAW_STAGE.ORDERS (
	ORDER_ID NUMBER(38,0),
	CUSTOMER_ID NUMBER(38,0),
	PRODUCT_ID NUMBER(38,0),
	ORDER_DATE DATE,
	QUANTITY NUMBER(38,0)
);

create or replace TABLE RETAIL_PROJECT.RAW_STAGE.TRANSACTIONS (
	TRANSACTION_ID NUMBER(38,0),
	ORDER_ID NUMBER(38,0),
	PAYMENT_METHOD VARCHAR(16777216),
	AMOUNT NUMBER(38,2),
	TRANSACTION_DATE DATE
);

create or replace TABLE RETAIL_PROJECT.RAW_STAGE.PRODUCTS (
	PRODUCT_ID NUMBER(38,0),
	NAME VARCHAR(16777216),
	CATEGORY VARCHAR(16777216),
	PRICE NUMBER(38,2)
);

create or replace TABLE RETAIL_PROJECT.TRANSFORMED.DIM_CUSTOMERS (
	CUSTOMER_ID NUMBER(38,0),
	NAME VARCHAR(16777216),
	EMAIL VARCHAR(16777216),
	CITY VARCHAR(16777216),
	CUSTOMER_SEGMENT VARCHAR(16777216)
);

create or replace TABLE RETAIL_PROJECT.TRANSFORMED.DIM_PRODUCTS (
	PRODUCT_ID NUMBER(38,0),
	NAME VARCHAR(16777216),
	CATEGORY VARCHAR(16777216),
	PRICE NUMBER(38,2)
);

create or replace TABLE RETAIL_PROJECT.TRANSFORMED.FACT_SALES (
	ORDER_ID NUMBER(38,0),
	CUSTOMER_ID NUMBER(38,0),
	PRODUCT_ID NUMBER(38,0),
	ORDER_DATE DATE,
	TRANSACTION_ID NUMBER(38,0),
	PAYMENT_METHOD VARCHAR(16777216),
	AMOUNT NUMBER(38,2),
	TRANSACTION_DATE DATE
);